import React from "react";

import ConfirmationPassword from "./ConfirmationPassword";
import { render } from "setupTests";

describe("ConfirmationPassword", () => {
  it("renders with default props", () => {
    render(<ConfirmationPassword />);
  });
});
