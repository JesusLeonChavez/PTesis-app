// Interfaces and types from component Register

/**
 * RegisterProps information.
 *
 * @interface RegisterProps
 * @since 1.0.0
 */

export interface RegisterProps {}
export interface RegisterMethods {
  email: string;
  password: string;
}
