import React from "react";

import Switch from "./Switch";
import { render } from "setupTests";

describe("Switch Component Test Suit", () => {
  it("Switch render component", () => {
    render(<Switch onPress={() => {}} />);
  });
});
